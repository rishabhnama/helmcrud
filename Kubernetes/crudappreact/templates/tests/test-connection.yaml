apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crudappreact.fullname" . }}-test-connection"
  labels:
    {{- include "crudappreact.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crudappreact.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
