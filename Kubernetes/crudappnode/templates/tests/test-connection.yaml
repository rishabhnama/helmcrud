apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crudappnode.fullname" . }}-test-connection"
  labels:
    {{- include "crudappnode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crudappnode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
